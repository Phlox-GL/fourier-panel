# Fourier panel migration follow-up

- Removed the legacy `compact.cirru` snapshot and kept `calcit.cirru` canonical.
- Pinned direct Phlox, touch-control, Respo, lilac, and memof inputs to verified commits.
- Updated CI to Node 24, Corepack/Yarn 4, canonical format validation, and `calcit` code generation.
- Verified immutable Yarn install, check-only, JS generation, and Vite production build.
